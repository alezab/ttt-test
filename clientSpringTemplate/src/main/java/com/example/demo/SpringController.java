package com.example.demo;

import org.springframework.web.bind.annotation.RestController;

/**
 * This is a RestController class in a Spring Boot application.
 * It is annotated with @RestController, which is a convenience annotation that adds @Controller and @ResponseBody.
 * This means that data returned by each method will be written straight into the response body instead of rendering a template.
 */
@RestController
public class SpringController {

}